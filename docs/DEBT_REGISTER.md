# Registro de Deuda Técnica y Operativa (V16)

Este documento centraliza la deuda técnica, operativa y funcional conocida del proyecto Opina+.
El mandato es visibilidad total: la deuda no se oculta, se clasifica rigurosamente entre **Aceptada temporalmente** (no bloquea el core) y **A corregir pronto** (riesgo estructural o promesa de producto incumplida).

> Sucesor de `DEBT_REGISTER_V13.md`. Actualizado tras Fase 2 de limpieza (Abril 2026).

---

## 1. Deuda activa

| ID | Área | Descripción real | Gravedad | Estado | Acción recomendada | Cuándo abordarlo |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| ~~**DEBT-001**~~ | ~~Tipados~~ | ~~Uso de `declare module` genérico para `framer-motion`, `lucide-react`, `chart.js`, `react-hot-toast`.~~ | — | **Cerrada en Fase 3.3** | Ver sección 2.2. | — |
| ~~**DEBT-002**~~ | ~~Frontend (B2C)~~ | ~~Superficie visible de módulos B2C que sugieren mayor profundidad funcional de la soportada actualmente por el read model subyacente.~~ | — | **Cerrada en Fase 4.3** | Ver sección 2.3. | — |
| ~~**DEBT-003**~~ | ~~Frontend (B2B)~~ | ~~B2B aún no completamente cerrado como producto comercial vendible; faltaba narrativeEngine real.~~ | — | **Cerrada en Fase 4.4** | Ver sección 2.3. | — |
| ~~**DEBT-004**~~ | ~~Arquitectura B2B~~ | ~~4 pages B2B pesadas (Overview, Benchmark, DeepDive, Reports).~~ | — | **Cerrada en Fase 3.3** | Ver sección 2.2. | — |
| ~~**DEBT-005**~~ | ~~Tests~~ | ~~3 suites E2E smoke en `e2e/`; falta CI wire-up, cobertura rate-limiter y visual regression.~~ | — | **Cerrada en Fase 4.1** | Ver sección 2.3. | — |
| ~~**DEBT-006**~~ | ~~Arquitectura (Backend)~~ | ~~Rate limiting backend ausente.~~ | — | **Cerrada en Fase 3.4** | Ver sección 2.2. | — |
| ~~**DEBT-007**~~ | ~~Estado Pendiente~~ | ~~Falta de sincronización offline robusta en caso de que un fetch se cierre antes de vaciar el local Outbox.~~ | — | **Cerrada en Fase 3.4** | Ver sección 2.2. Migración a IndexedDB + Service Worker queda como mejora futura (no bloqueante). | — |
| ~~**DEBT-008**~~ | ~~Linting~~ | ~~16 errores ESLint vivos al cierre de Fase 1.~~ | — | **Cerrada en Fase 2.1** | Ver sección 2. | — |
| ~~**DEBT-009**~~ | ~~Design System (Tailwind)~~ | ~~Aliases legacy `primary`/`secondary` + `text-text-*` vivos.~~ | — | **Cerrada en Fase 4.2** | Ver sección 2.3. | — |

## 2. Deuda cerrada en Fase 1 (Abril 2026)

| ID | Qué era | Cómo se cerró |
| :--- | :--- | :--- |
| ~~DEBT-TAILWIND-REGRESIÓN~~ | 47 clases doble-opacidad (`bg-brand/20/50`, `ring-brand/50/10`) introducidas por `replace_primary.js` | Script `scripts/repo/fix-tailwind-palette.mjs` aplicado: restauró forma con shade (`bg-brand-100/50`) preservando color original vía `colors.blue`/`colors.emerald`. |
| ~~DEBT-TAILWIND-LEGACY~~ | 31 clases `primary-*`/`secondary-*` sobrevivientes que la migración anterior no tocó (`src/features/auth`, `src/features/b2b`, shadows) | Migradas a `brand-*`/`accent-*` en el mismo script. Grep ahora retorna 0 ocurrencias. |
| ~~DEBT-ROOT-SUCIA~~ | `audit_text.txt` (vacío), `check_policy.sql`, `check_policy2.sql`, `replace_primary.js`, `Copia de Opina_Maestro_Comercial_v8.pdf` en raíz + 4 docs de investigación (`01-04 Opina+ ...`) fuera de `docs/` | Archivos temporales eliminados; investigación movida a `docs/research/` con nombres estandarizados. |
| ~~DEBT-AUDITORIAS-CONTRADICTORIAS~~ | Dos auditorías aparentemente contradictorias conviviendo sin jerarquía (`full-project-audit-master.md` vs `final_audit_conclusions.md`) | Ambas marcadas como HISTÓRICO con banner al inicio apuntando al estado vivo en `PROJECT_MASTER_STATUS.md` y este registro. |

## 2.4 Mejoras Fase 5 (Abril 2026) — post-cierre de deuda

Fase 5 no cierra deuda (la tabla principal ya está en 0); son mejoras de cara al producto B2B y a la postura operativa. Se documentan para que no pasen como "cambios silenciosos".

| Área | Mejora | Qué se hizo |
| :--- | :--- | :--- |
| E2E | Cobertura expandida: tests de regresión sobre cierres de Fase 3.4, 4.3 y 4.4 | 3 specs nuevos: (1) `e2e/hub-beta-badge.spec.ts` — verifica que al menos un track del Hub Bento Grid muestra badge "Beta" en `/` (protege DEBT-002: cualquier refactor que pierda el flag `beta: true` en `hubSecondaryData.ts` falla aquí aunque pase types). (2) `e2e/outbox-offline.spec.ts` — emite una señal con `insert_signal_event` mockeado a 500, verifica que `localStorage['opina_signal_outbox_v1']` contiene un job bien formado con `id/rpc/createdAt/attempts/nextAttemptAt` (protege DEBT-007: el wiring UI→outbox→localStorage no se puede validar por tests unitarios del módulo aislado). (3) `e2e/b2b-reports-narrative.spec.ts` — inyecta un snapshot con `availability: 'sufficient'` + `exportStatus: 'ready'` + 2 entries con líder dominador, verifica que `ReportsB2B` renderiza "Brief Ejecutivo" y NO muestra el banner Beta histórico ("plantilla sobre tus métricas" / "no por un modelo de IA") ni el card insufficient_data (protege DEBT-003 cierre de Fase 4.4). Total suite E2E: 8 specs (antes 5 · happy path, rate limit, overview, access gate, visual). Typecheck y lint limpios. |
| Observabilidad | **Baseline** de reporte de errores desacoplado y captura global | Nuevo módulo `src/lib/observability/`: (1) `errorReporter.ts` define la interfaz `ErrorReporter` (`captureException`, `captureMessage`) + contexto `ErrorContext` + `ConsoleErrorReporter` (default, delega al `logger` ya existente con sanitización) + setters `setErrorReporter`/`resetErrorReporter` para swap (mismo patrón que `narrativeProvider` de Fase 5.1). Sumar Sentry/Datadog mañana = 1 archivo. (2) `windowErrorBridge.ts` registra `window.addEventListener('error', ...)` + `'unhandledrejection'` y los reporta vía `getErrorReporter().captureException(...)`. Instalación **idempotente** (re-llamar `installWindowErrorBridge()` no duplica listeners, crítico para HMR y tests), con `uninstallWindowErrorBridge()` para cleanup. Los handlers tragan errores del propio sink para nunca romper al app desde el handler global. Wiring: `index.tsx` llama `installWindowErrorBridge()` al bootstrap y retira el listener ad-hoc que había para `unhandledrejection`. Los 3 `ErrorBoundary` (Global/Module/genérico) migraron de `logger.error(...)` directo a `getErrorReporter().captureException(...)` — mismo sink hoy, pero las capas de arriba ya no saben del transport. Tests nuevos: `errorReporter.test.ts` (7 casos: default = console-v1, captureException tolera Error/string/null/undefined, swap cambia el sink, mock recibe las llamadas con el contexto, reset restaura default), `windowErrorBridge.test.ts` (6 casos: instalación + idempotencia, captura de `error` event síncrono con metadata, captura de `unhandledrejection`, uninstall limpia listeners, sink que lanza no rompe la página). Golden Rule OK. |
| Accesibilidad (B2C) | Auditoría + fixes **WCAG 2.1 AA** sobre superficies B2C críticas | Nuevo hook reutilizable `src/hooks/useModalA11y.ts` (escape-to-close + restaurar foco al elemento previo al desmontar + foco inicial al contenedor con `tabindex=-1`; `skipInitialFocus` opcional cuando el modal contiene un control que debe recibir foco por su cuenta). Aplicado en los 5 modales B2C: `RequestLoginModal`, `GuestConversionModal`, `ProfileRequiredModal`, `BatchSessionResults`, `LugarSignalWizard`. `role="dialog"` + `aria-modal="true"` + `aria-labelledby`/`aria-label` agregados donde faltaban (todos los 5). `aria-label` explícito en botones icon-only (botón back de `VersusView`, botón close de `BatchSessionResults` y `LugarSignalWizard`). `aria-hidden="true"` en todos los íconos `material-symbols-*` decorativos dentro de dichos componentes (evita que lectores de pantalla lean texto literal como "arrow_back"/"close"). En `HubBentoGrid`: **eliminado el `<h3>` anidado dentro de `<button>`** (violación ARIA — buttons no permiten headings) reemplazándolo por `<span class="block ...">` con misma escala tipográfica; `aria-label="{título}: Próximamente"` en las cards deshabilitadas para que el screen reader entienda por qué no responden. Tests: `useModalA11y.test.ts` con 5 casos (escape dispara onClose, otras teclas no, listener sólo se monta si isOpen, listener se desmonta al cerrar, foco se restaura al elemento previo al unmount). Golden Rule OK (typecheck + lint limpios). Focus trap completo queda documentado como mejora futura (requiere `focus-trap-react` o enumeración manual de focusables; los modales actuales son chicos y el patrón actual cubre WCAG 2.1.1 Keyboard + 2.4.3 Focus Order). |
| Narrativa B2B | Abstracción **LLM-ready** sobre `narrativeEngine` | Nuevo `src/features/b2b/engine/narrativeProvider.ts` con interfaz `NarrativeProvider` (dos métodos async: `generateEntityNarrative` y `generateMarketNarrative`). `DefaultNarrativeProvider` envuelve el rule-based determinístico de Fase 4.4 vía `Promise.resolve(...)`. Factoría `getNarrativeProvider()` + setters `setNarrativeProvider` / `resetNarrativeProvider` permiten swap en runtime (feature flag) o en tests. Consumidores migrados: `useBenchmarkB2BState.handleSelectEntity` hace `await getNarrativeProvider().generateEntityNarrative(...)`, y `reportsHelpers.buildReportContent` es ahora `async` y hace `await getNarrativeProvider().generateMarketNarrative(...)`; `ReportsB2B` adoptó `useState<ReportContent \| null>` + `useEffect` para tolerar proveedores con latencia (muestra "Generando Brief Ejecutivo..." mientras resuelve). Tests nuevos en `narrativeProvider.test.ts`: default devuelve `name='rule-based-v1'`, swap cambia el provider activo, mock async con `setTimeout(10ms)` no rompe el contrato, y un provider con fallback (`try { throw } catch { return Default... }`) correctamente delega al rule-based. **Razón de no sumar el LLM hoy**: el rule-based ya da salida accionable, reproducible y sin costo; la capa queda lista para que cuando haya presupuesto de API, el cambio sea `setNarrativeProvider(new LLMNarrativeProvider())` desde el bootstrap, sin tocar consumidores ni la UI. El rule-based queda como fallback + ground truth para evals. |

## 2.3 Deuda cerrada en Fase 4 (Abril 2026)

| ID | Qué era | Cómo se cerró |
| :--- | :--- | :--- |
| ~~DEBT-003-NARRATIVE~~ | Fase 3.2 había blindado los dos síntomas visibles de la sobrepromesa B2B (`ReportsB2B` con banner Beta declarando "narrativa por plantilla, no IA"; `BenchmarkB2B` con `MetricAvailabilityCard` status `pending` en lugar del empty state fake), pero la raíz seguía intacta: (a) `useBenchmarkB2BState.handleSelectEntity` hacía `setEntityNarrative(null)` con un TODO que nadie iba a cerrar; (b) `reportsHelpers.buildReportContent` interpolaba strings ad-hoc sobre el primer elemento del snapshot sin clasificación alguna. Ambos banners Beta eran honestos sólo porque lo que había debajo era débil. | Nuevo módulo `src/features/b2b/engine/narrativeEngine.ts` con motor narrativo determinístico: (1) `classifyEntity(entry)` evalúa en orden 8 categorías (`insufficient_sample` → `market_dominator` → `contested_leader` → `rising_challenger` → `falling_incumbent` → `volatile_player` → `stable_challenger` → `fringe`), primera que matchea gana; clasificación trazable a reglas sobre `leaderRank`, `marginVsSecond`, `stabilityLabel`, `nEff`. (2) `computeConfidence(entry)` asigna `Alta`/`Media`/`Baja` según ancho del Wilson CI + nEff — independiente de la categoría. (3) `generateEntityNarrative(entry)` devuelve `BenchmarkSystemNarrative` con texto por plantilla-por-categoría (no interpolación genérica), `confidence`, `category` (label corto) y `backingMetrics.deltaPercentage` real (marginVsSecond para líderes, `(1 - entropyNormalized)` como proxy de concentración para el resto). (4) `generateMarketNarrative({entries, highAlertMessage})` arma los 4 bloques del reporte ejecutivo (summary/findings/criticalAlert/strategicRecommendation) con las mismas reglas. Wiring: `useBenchmarkB2BState` guarda `benchmark.entries` del snapshot y en `handleSelectEntity` llama `generateEntityNarrative`; `reportsHelpers.buildReportContent` delega a `generateMarketNarrative`. Retirados: `BetaDisclaimerBanner` de `ReportsB2B` (narrativa ya no es "plantilla ad-hoc"), `MetricAvailabilityCard status='pending'` del panel cambiado a `insufficient_data` (semántica honesta: sin entry canónica no hay narrativa). Tests: `narrativeEngine.test.ts` con 22 casos cubriendo las 8 categorías en sus bordes + cada nivel de confianza + 7 casos de narrativa de mercado. Razón de ser rule-based (no LLM): reproducibilidad frente a un mismo snapshot, auditabilidad caso-a-caso, cero dependencia de proveedor externo, cero costo por token. Cuando se sume LLM, este engine queda como fallback + ground truth. DEBT-003 cerrada. |
| ~~DEBT-002-RESTO~~ | Fase 3.2 había cerrado Hub Bento Grid + `LugarSignalWizard` + banners en `LugaresView`/`ServiciosView`, pero quedaban dos vectores no auditados: (a) `ProfundidadView` end-to-end, (b) `ServicioDetailView` — el modal que se abre al tocar un servicio. Auditoría de Fase 4.3 encontró: **ProfundidadView está limpio** — delega a `InsightPack`, que consulta `depth_definitions`/`battle_options` por RPC y llama `depthService.saveDepthStructured`, el cual escribe tanto a `signal_events` via `signalService.saveSignalEvent` (canónico) como al RPC legacy `insert_depth_answers`. Persistencia real, sin sobrepromesa. **`ServicioDetailView` sí tenía crímenes**: (1) una sección "Evaluación de la Comunidad · Últimos 10 Usuarios" con valores **hardcodeados** (`service.rating > 4 ? 4.8 : 3.8` para Confiabilidad, `4.5/3.2` para Resolución, `4.2/2.5` para Cobros) presentados como telemetría real, incluyendo un dot `animate-pulse` con texto "Tendencia Estable" que sugería actividad en vivo; (2) CTA principal "Evaluar este Servicio" con `onClick={() => {}}` — mudo. | Refactor de `src/features/feed/components/ServicioDetailView.tsx`: (1) Bloque "Evaluación de la Comunidad" reemplazado por `BetaDisclaimerBanner` contextual ("Señales de Servicios en preparación") + 3 `MetricAvailabilityCard` con `status='pending'` (Confiabilidad, Resolución, Cobros) — mismo patrón que Fase 3.2 usó en `BenchmarkB2B`. Se eliminó el avatar "Últimos 10 Usuarios" + dot pulsante + promedio falso. (2) CTA renombrado a "Avísame cuando pueda evaluar" y cableado a `handleEvaluateInterest()`, que invoca `moduleInterestService.trackModuleInterestEvent(MODULE_INTEREST_CLICKED, ...)` con `module_slug='servicios'` + `value=service.id` y confirma con toast honesto ("Gracias, registramos tu interés. La captura de señales para Servicios llega en la próxima versión."). Rate-limit diario per-servicio via `rateLimit.hasSent(...:daily:)` evita spam. Resultado: la ficha no miente sobre datos que no existen, y el gesto del usuario sí produce un evento real útil para priorizar roadmap. DEBT-002 cerrada end-to-end. |
| ~~DEBT-005-CI~~ | Los 3 specs Playwright corrían sólo local (`playwright.config.ts` con `webServer` comentado y `validate.yml` sin step E2E). Faltaba cobertura de UX de rate-limit y cualquier forma de visual regression. | (1) `playwright.config.ts`: activado `webServer` con `reuseExistingServer: !CI` (reutiliza dev server en local; CI lo levanta). Timeout 120s. (2) `.github/workflows/validate.yml` partido en dos jobs: `static-checks` (typecheck+lint+test:run+build) y `e2e` (depende del anterior, instala chromium only, corre `npm run test:e2e`, sube `playwright-report/` always y `test-results/` on-failure). (3) Nuevo `e2e/versus-rate-limit.spec.ts`: mockea `insert_signal_event` → RATE_LIMITED y verifica que el toast "Vas muy rápido" se renderiza (no el fallback genérico "Error DB"). (4) Nuevo `e2e/hub-visual.spec.ts`: scaffold de visual regression gated por `E2E_VISUAL=1`, con `toHaveScreenshot()` + `maxDiffPixelRatio: 0.01` + `animations: 'disabled'`; bootstrap documentado en el header (baselines se generan con `--update-snapshots` y se commitean). Total: 6 tests registrados (5 activos + 1 skip-hasta-baseline). DEBT-005 cerrada. |
| ~~DEBT-009-TAILWIND~~ | `tailwind.config.js` aún tenía `primary: '#2563EB'` y `secondary: '#10B981'` como scalars legacy, más las claves `text-primary`/`text-secondary`/`text-muted` que generaban las utilities `text-text-*`. 284 usos regados por `src/` (161 en `text-text-*`, 123 en `bg-primary`/`hover:text-primary`/etc.). Mientras los aliases vivieran, cualquier componente nuevo podía caer en la rama legacy sin que el linter lo notara. | Script `scripts/repo/fix-tailwind-tokens.mjs`: recorre `src/**/*.{ts,tsx,js,jsx,css,html}`, aplica regex con guards (`btn-primary`/`badge-primary`/`var(--primary)` quedan intactos por ser nombres de componentes CSS o variables) y migra: (a) `{bg,text,border,ring,shadow,fill,stroke,divide,outline,decoration,from,to,via,border-[lrtbxy]}-primary(/\\d+)?` → `…-brand…`, idem `secondary` → `accent`, con soporte de modificadores (`hover:`, `md:`, `group-hover:`, etc.); (b) `bg-primary-dark`/`bg-primary-hover` → `bg-brand-700` (eran clases muertas que no pintaban nada porque `primary` era scalar); (c) `text-text-secondary` → `text-slate-600`, `text-text-muted` → `text-slate-500` (mismos valores, nombre semántico). Resultado: 49 archivos tocados, 527 sustituciones. Retiradas de `tailwind.config.js` las 5 claves legacy (`primary`, `secondary`, `text-primary`, `text-secondary`, `text-muted`). Grep post-migración: 0 utilities con `-primary`/`-secondary`/`text-text-*`. DEBT-009 cerrada. |

## 2.2 Deuda cerrada en Fase 3 (Abril 2026)

| ID | Qué era | Cómo se cerró |
| :--- | :--- | :--- |
| ~~DEBT-SURFACE-AUDIT~~ | No existía un mapa de qué módulos eran reales, parciales o scaffolding en B2C/B2B. Cualquier decisión de "construir/ocultar/etiquetar Beta" era adivinanza. | `docs/audit/phase3-surface-diagnostic.md`: auditoría estática del código con evidencia `archivo:línea` por módulo. Veredicto explícito por cada track B2C del Hub Bento Grid y por cada página B2B. Base formal para Fase 3.2. |
| ~~DEBT-002-CRED-B2C~~ | El Hub Bento Grid exponía `lugares` y `servicios` como tracks activos sin disclaimer, y `LugarSignalWizard` mostraba "¡Señal Capturada!" sobre datos que se silenciaban (onComplete vacío). El CTA "Solicitar lanzamiento" en `ComingSoonModule` ejecutaba `alert()` cosmético. | (1) `TrackCard.beta` + marca en `HubBentoGrid.tsx` → etiqueta BETA visible. (2) `BetaDisclaimerBanner` nuevo, inyectado al tope de `LugaresView` y `ServiciosView` con copy honesto sobre limitaciones de datos. (3) Success screen del wizard reescrita: "Gracias por evaluar" + pill "Lugares · Beta" + explicación de que la agregación pública llega pronto. (4) `ComingSoonModule` reemplaza `alert()` por `useToast` + `moduleInterestService.trackModuleInterestEvent(MODULE_INTEREST_CLICKED)` con dedup por render. Resto de DEBT-002 bajó de Alta a Media. |
| ~~DEBT-003-CRED-B2B~~ | `ReportsB2B` presentaba un "Resumen Ejecutivo" y una "Recomendación Táctica C-Level" generadas por string interpolation que parecían salida de IA. `BenchmarkB2B` hardcodeaba `setEntityNarrative(null)` y el fallback italic "No hay datos suficientes…" quedaba permanente, sin indicar que era una feature por construir. | (1) `ReportsB2B` inyecta `BetaDisclaimerBanner` al tope del documento A4 declarando explícitamente "los textos se generan por plantilla sobre tus métricas actuales, no por un modelo de IA". (2) `BenchmarkB2B` — cuando no hay narrativa real, se renderiza un `MetricAvailabilityCard` con status `pending` y helperText "El motor narrativo para esta vista está en desarrollo", en vez del empty state ambiguo. El render premium sólo sucede si `entityNarrative` es non-null. Resto de DEBT-003 bajó de Alta a Media. |
| ~~DEBT-004-B2B-PAGES~~ | `BenchmarkB2B` (296 líneas), `DeepDiveB2B` (215), `ReportsB2B` (223) mezclaban data shaping, lógica de selección y 3+ secciones de UI en un solo archivo. Dificultaba tests unitarios y hacía invisibles los casos borde (sin líder, sin retador, snapshot insufficient_data). | Aplicado el mismo patrón que Fase 2.3 (OverviewB2B): **Benchmark** → `useBenchmarkB2BState` + `benchmarkHelpers` (mapSnapshotToLeaderboard, filterLeaderboardByName) + `BenchmarkB2BHeader`/`RankingTable`/`DeepDivePanel`; página queda en 57 líneas. **DeepDive** → `deepDiveHelpers.computeDeepDivePair` (pure, devuelve null si insuficiente) + `DeepDiveB2BHeader`/`ContestantCard` (variant leader/challenger)/`InsightPanel`; página en 72 líneas. **Reports** → `reportsHelpers.buildReportContent` + `isReportAvailable` + `ReportsB2BHeader`/`ReportsB2BDocument`; página en 71 líneas. Las 4 pages B2B ahora son composers de <90 líneas. DEBT-004 cerrada. |
| ~~DEBT-001-TYPES~~ | `src/declarations.d.ts` contenía `declare module 'framer-motion'`, `declare module 'lucide-react'`, `declare module 'chart.js'`, `declare module 'react-hot-toast'` — todos genéricos, que eclipsaban los tipos reales que traen estas librerías en su propio `dist/*.d.ts`. Efecto: toda API de `motion`, `AnimatePresence`, `Icon`, `ChartConfiguration`, `toast()` era `any`. | Verificado que los 4 paquetes instalados (`framer-motion@11`, `lucide-react@0.563`, `chart.js@4.4`, `react-hot-toast@2.6`, + `react-chartjs-2@5.2`) exponen `package.json.types` apuntando a tipos oficiales. Se retiraron los 4 `declare module` del `declarations.d.ts` y el archivo quedó como gate documentando la decisión. `npm run typecheck && npm run lint` siguen en 0 errores — las APIs ya estaban siendo usadas de forma compatible con los tipos reales; sólo estaban silenciadas. DEBT-001 cerrada. |
| ~~DEBT-006-BACKEND~~ | El `signalRateLimiter` cliente (token bucket, 40 signals/min versus/progressive/pulse, 100 depth/news) era la única defensa real contra bursts. El RPC `insert_signal_event` sólo tenía el cooldown de 5 min por battle y el daily cap 20 por no-verified, pero nada que frene 300 señales/min en distintos battles. `grant_pilot_access` no tenía anti brute-force. | Migración `20260422000000_debt006_backend_rate_limiting.sql`: (1) función `enforce_signal_rate_limit(user_id, anon_id, module_type)` que replica exactamente los caps del cliente con una ventana deslizante de 60s sobre `signal_events`; levanta `RATE_LIMITED` (ya listado como no-retriable en `signalOutbox.isNonRetriableSignalErrorMessage`). (2) Nueva tabla `pilot_access_attempts` (RLS admin-only) + función `enforce_pilot_access_rate_limit()` (5 intentos/min por `auth.uid()`); `grant_pilot_access` ahora audita cada intento (éxito o fallo) y rechaza cuando exceden. `insert_signal_event` reescrito con `PERFORM public.enforce_signal_rate_limit(...)` antes del cooldown por battle. DEBT-006 cerrada end-to-end (cliente + backend alineados en los mismos límites). |
| ~~DEBT-007-OUTBOX~~ | `signalOutbox.ts` ya tenía dedupe por `p_client_event_id`, backoff exponencial y clasificación de errores no-reintentables, pero carecía de: (a) contrato documentado al tope del archivo, (b) jitter en el backoff (retry-storm al volver online simultáneo), (c) flush pre-unload (la pestaña se cerraba con jobs en queue), (d) superficie de telemetría, y (e) tests unitarios. | Refactor en `src/features/signals/services/signalOutbox.ts`: (1) Docstring de contrato al tope enumerando las 6 garantías (idempotencia, backoff+jitter, clasificación, retención 7d/500, triggers de flush, observabilidad). (2) `backoffMs` ahora suma `Math.random() * min(5000, base * 0.25)` al resultado — desfasa curvas de retry entre clientes. (3) `startSignalOutbox` registra `pagehide` + `beforeunload` además de `online`/`visibilitychange`/intervalo. (4) Exportada `getOutboxStats()` que devuelve `{total, retriable, nonRetriable, oldestCreatedAt, nextAttemptAt}` — base para un badge "N señales pendientes". (5) Nuevo `signalOutbox.test.ts` con 14 tests cubriendo dedupe, UUID auto, filtros per-session, flush exitoso, drop non-retriable, retry con backoff, respeto de `nextAttemptAt`, backoff bounds + jitter y `getOutboxStats`. Migración a IndexedDB + Service Worker queda explícitamente documentada como mejora futura (no bloqueante: 500 jobs cabe sobrado en localStorage). DEBT-007 cerrada. |

## 2.1 Deuda cerrada en Fase 2 (Abril 2026)

| ID | Qué era | Cómo se cerró |
| :--- | :--- | :--- |
| ~~DEBT-008~~ | 16 errores ESLint vivos: 13 `any` residuales + 2 `exhaustive-deps` + 1 `any` extra. | Tipado real caso por caso: `logger.sanitizePayload` devuelve `unknown`; `EntityMetadata` con `[key: string]: unknown`; `B2BAnalyticsOption` definido en `insightsTypes`; `moduleInterestService` usa `Database['public']['Functions']['track_module_interest']['Args']`; `useExperienceMode` importa `BehaviorModuleType`; hooks reparados con `useCallback` + deps correctas; `catch (err)` → `catch` cuando `err` no se usa. 0 errores, 0 warnings. |
| ~~DEBT-BOUNDARIES-LOCALES~~ | Solo existía `GlobalErrorBoundary` + 6 boundaries en SignalsRouter. Admin, B2B y Pilot Launchpad crasheaban al top-level ante cualquier error en su subtree. | Añadido `ModuleErrorBoundary` dentro de `B2BLayout` (envuelve los 5 sub-routes), dentro de `AdminSectionLayout` (envuelve las 4 secciones tabuladas con sus 11 sub-páginas) y en la ruta standalone `/admin/demo`. Cobertura total: 3 capas de aislamiento (global → sección → módulo). |
| ~~DEBT-RATE-LIMIT-CLIENT~~ | Sin control client-side de ráfagas sobre `insert_signal_event` (parte de DEBT-006). | `signalRateLimiter.ts` con token bucket per-módulo: `MIN_INTERVAL_MS=250`, caps 40/min (versus, progressive, pulse) y 100/min (depth, news). Integrado en `signalWriteService.saveSignalEvent` **antes** del outbox para evitar llenar `localStorage` con jobs condenados. Error propagado como `SignalRateLimitError` (code `RATE_LIMITED`); UX muestra toast "Vas muy rápido..." en VersusView/TorneoView; outbox lo lista como non-retriable. 7 tests unitarios en `signalRateLimiter.test.ts`. **Backend sigue pendiente (DEBT-006).** |
| ~~DEBT-OVERVIEW-B2B-MASIVO~~ | `OverviewB2BExecutiveSummary.tsx` (144 líneas) mezclaba data shaping + 3 secciones de UI. | Extraído a: `utils/executiveSummaryHelpers.ts` (puro, testeable), `OverviewB2BExecutiveNarrative.tsx` (48 líneas), `OverviewB2BKeyFindings.tsx` (45), `OverviewB2BSignalsRail.tsx` (41). El composer quedó en 26 líneas. Parcialmente cierra DEBT-004 (ver scope restante). |

---

## 3. Política de Deuda

1. **Deuda Aceptada Temporalmente**: Implica que la decisión de tomar el atajo fue consciente para ganar velocidad en el bloque de estabilización anterior. **No** es una licencia para perpetuar el hack.
2. **Deuda A Corregir Pronto**: Ítems cuya mera existencia compromete métricas de negocio, seguridad, o la percepción del cliente final (sobrepromesa). Su priorización en el backlog es obligatoria.
3. **Regla de oro**: antes de cerrar un ítem de esta lista, correr el Golden Rule del `README.md` (`npm run typecheck && npm run test:run && npm run build`) y dejar evidencia en la columna "Cómo se cerró" de la sección 2.
